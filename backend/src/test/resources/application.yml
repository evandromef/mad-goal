spring:
  datasource:
    url: jdbc:h2:mem:mad;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  jwt:
    secret: test-secret-that-has-more-than-thirty-two-characters-long
    expiration-minutes: 60
  cors:
    allowed-origin: http://localhost:4200

